public class Main {

    public static void main(String[] args) {

        /* просто тесты
        tasks.Task task = new tasks.Task("Первая задача", "ОБЫЧНАЯ", "NEW");
        manager.Manager manager = new manager.Manager();

        manager.createTask(task);
        System.out.println(manager.getAllTasks().get(0).getStatus());

        tasks.Task task1 = new tasks.Task("Вторая задача", "интересная", "NEW");

        manager.createTask(task1);
        System.out.println(manager.getAllTasks().get(1).getId());

        tasks.Epic epic = new tasks.Epic("Первый эпик", "обычный", "NEW");

        manager.createEpic(epic);
        System.out.println(manager.getEpicById(epic.getId()).description);

        tasks.Subtask subtask = new tasks.Subtask("Первая подзадача", "Необычная", "NEW", epic.getId());

        manager.createSubtask(subtask);
        System.out.println(manager.getSubtasksOfEpic(epic).get(0).getId());

        tasks.Subtask subtask1 = new tasks.Subtask("Вторая подзадача", "Скучная", "IN_PROGRESS", epic.getId());

        manager.createSubtask(subtask1);

        tasks.Subtask subtaskUp = new tasks.Subtask("Первая подзадача", "Необычная", "DONE",
                epic.getId());

        manager.updateSubtask(subtaskUp);
        System.out.println(manager.getEpicById(epic.getId()).getStatus());
*/
    }
}
